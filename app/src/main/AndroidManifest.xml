<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devdesign.developer.ziptown">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyChzL_RETVgMiVI4poAZDP9OUDltyyweKU" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activities.MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activities.UserProfileActivity" />
        <activity android:name=".activities.CurrentUserActivity" />
        <activity android:name=".activities.AddGenericPostActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.signupActivities.SignUpActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.LandingPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SearchResults" />
        <activity
            android:name=".activities.NetworkIssuesActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activities.signupActivities.NumberActivity" />
        <activity android:name=".activities.signupActivities.VerifyNumberActivity" />
        <activity android:name=".activities.signupActivities.PasswordActivity" />
        <activity android:name=".activities.signupActivities.FullNameActivity" />
        <activity android:name=".activities.signupActivities.CityActivity" />
        <activity android:name=".activities.signupActivities.DriverOrPassengerActivity" />
        <activity android:name=".activities.signupActivities.TermsAndConditionsActivity"></activity>
    </application>

</manifest>